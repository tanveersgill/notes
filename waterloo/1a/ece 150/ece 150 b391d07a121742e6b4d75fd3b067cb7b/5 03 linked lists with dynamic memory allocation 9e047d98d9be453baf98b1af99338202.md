# 5.03 linked lists with dynamic memory allocation

if we dynamically allocate memory every time we need a new node:

- we store the address of the next node

***************************************************************************************(rest of notes in my notebook)***************************************************************************************